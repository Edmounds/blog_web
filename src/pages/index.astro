---
import SpaLayout from "../layouts/SpaLayout.astro";
import { getSiteSettings } from "../lib/content";

export const prerender = true;

const site = await getSiteSettings();
---
<SpaLayout title="Deploy Test" description="Design notes, essays, and selected projects." initialPath="/" />
<meta name="deployment-marker" content="github-actions-20260723-visible" />
