---
import SpaLayout from "../layouts/SpaLayout.astro";
import { getLocalizedSite } from "../lib/localized-content";

export const prerender = true;
const site = await getLocalizedSite("zh-CN");
---

<SpaLayout
  title="last4ev4r"
  description={site.page.homeDescription}
  initialPath="/"
/>
