---
import SpaLayout from "../../layouts/SpaLayout.astro";
import { getLocalizedSite } from "../../lib/localized-content";

export const prerender = true;

const localized = await getLocalizedSite("zh-CN");
---
<SpaLayout title={localized.labels.about} description={localized.page.aboutDescription} initialPath="/about/" />
