---
import { Card } from "../ui/card";

interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<Card className={`ui-panel !gap-0 !py-0 !ring-0 ${className}`.trim()}>
  <slot />
</Card>
