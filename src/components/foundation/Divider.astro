---
import { Separator } from "../ui/separator";

interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<Separator className={`ui-divider !bg-[color:color-mix(in_srgb,var(--border-strong)_75%,transparent)] ${className}`.trim()} />
