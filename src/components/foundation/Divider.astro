---
import { Separator } from "../ui/separator";

interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<Separator className={`ui-divider !bg-[var(--border-soft)] ${className}`.trim()} />
